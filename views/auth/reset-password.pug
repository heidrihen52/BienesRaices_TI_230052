extends ../layout/index

block contenido
    div.py-10
        h1.text-4xl.my-10.font-extrabold.text-center.text-green Bienes 
            span.font-normal.text-orange Raíces  
        h2.text-center.text-2xl.font-extrabold.text-green= pagina

        if errores 
            div(class="max-w-md mx-auto my-10")
                each error in errores
                    p.bg-red-600.text-white.uppercase.text-xs.text-center.p-2.mb-1.font-bold= error.msg

        .mt-8.mx-auto.max-w-md 
            .bg-white.py-8.px-4.shadow
                form.space-y-5(method="POST" novalidate) 
                    input(type="hidden" name="_csrf" value= csrfToken)
                    div 
                        label.block.text-sm.uppercase.text-gray.mb-3.font-bold(for="password") Coloca tu nuevo Password
                        input#passwordl.w-full.px-3.py-2.border.border-gray.rounded-md.placeholder-gray(placeholder="Escribe tu nuevo password" type="password" name="password") 
                   
                    input(class="w-full bg-green hover:bg-orange text-white font-bold py-3 cursor-pointer" type="submit" value="Guardar Password")    
